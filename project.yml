# project.yml — xcodegen configuration for HouseholdApp
# Run `xcodegen generate` from the project root to regenerate HouseholdApp.xcodeproj.
# Install xcodegen: brew install xcodegen

name: HouseholdApp

options:
  bundleIdPrefix: com.householdapp
  deploymentTarget:
    iOS: "17.0"
  defaultConfig: Debug
  createIntermediateGroups: true
  # Keeps Xcode from reordering your source files alphabetically.
  groupSortPosition: none

# ── Targets ────────────────────────────────────────────────────────────────────
targets:
  HouseholdApp:
    type: application
    platform: iOS
    deploymentTarget: "17.0"

    # ── Source files ───────────────────────────────────────────────────────────
    # xcodegen auto-discovers all .swift files and resources under HouseholdApp/.
    sources:
      - path: HouseholdApp
        # Exclude the entitlements file from compilation (it's a resource).
        excludes:
          - "**/*.entitlements"

    # ── Resources ──────────────────────────────────────────────────────────────
    resources:
      - path: HouseholdApp/Assets.xcassets
      - path: HouseholdApp/Info.plist

    # ── Build settings ─────────────────────────────────────────────────────────
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.householdapp.app
        SWIFT_VERSION: "5.9"
        # Target both iPhone and iPad.
        TARGETED_DEVICE_FAMILY: "1,2"
        INFOPLIST_FILE: HouseholdApp/Info.plist
        CODE_SIGN_ENTITLEMENTS: HouseholdApp/HouseholdApp.entitlements
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"
        # Use automatic signing — fill in your team ID or use Xcode UI.
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: ""
      configs:
        Debug:
          DEBUG_INFORMATION_FORMAT: dwarf
          SWIFT_OPTIMIZATION_LEVEL: "-Onone"
          SWIFT_ACTIVE_COMPILATION_CONDITIONS: DEBUG
        Release:
          DEBUG_INFORMATION_FORMAT: dwarf-with-dsym
          SWIFT_OPTIMIZATION_LEVEL: "-O"

    # ── Entitlements ───────────────────────────────────────────────────────────
    entitlements:
      path: HouseholdApp/HouseholdApp.entitlements

    # ── Linked frameworks ──────────────────────────────────────────────────────
    # SwiftUI, CoreData, and CloudKit are automatically linked by Xcode.
    # No explicit framework entries needed.

# ── Schemes ────────────────────────────────────────────────────────────────────
schemes:
  HouseholdApp:
    build:
      targets:
        HouseholdApp: all
    run:
      config: Debug
    test:
      config: Debug
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
